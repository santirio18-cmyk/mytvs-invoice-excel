# Public site URL of your Vercel frontend (comma-separated). Example:
# CORS_ORIGINS=https://mytvs-invoices.vercel.app,http://localhost:3000
CORS_ORIGINS=*

# Optional: where temp Excel/CSV copies are written (defaults to /tmp/output on cloud)
# OUTPUT_DIR=/tmp/output
